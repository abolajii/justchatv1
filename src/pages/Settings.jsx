import React from "react";
import styled from "styled-components";
import { MainContainer } from "../components";

const Settings = () => {
  return <MainContainer>Settings Page</MainContainer>;
};

export default Settings;
