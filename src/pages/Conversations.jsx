import React from "react";
import styled from "styled-components";
import { MainContainer } from "../components";

const Conversations = () => {
  return <MainContainer>Conversations Page</MainContainer>;
};

export default Conversations;
